# neuro-genai-loop

EEG brain-state engine and adaptive tutor (work in progress).

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Place EEG files in `data/`.
